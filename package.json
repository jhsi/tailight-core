{
  "name": "trailwind-core",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "examples:dev": "npm run build && cd examples && npm run dev",
    "examples:build": "npm run build && cd examples && npm run build",
    "examples:preview": "cd examples && npm run preview"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {}
}